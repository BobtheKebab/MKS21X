# To clone a directory from github onto the local machine
# x = text that github gives you for cloning when looking at a repository
git clone "x"

# To stage an item in preparation for committing
# Must be done for newly added files or editted files
git add "filename"

# To commit a version
# Must have a message accompanying it
git commit -m "message"

# To push all commits to GitHub
git push
